# Review vom 2026-08-10 (Fokus: Neue Features)

Zweiter Lauf am 2026-08-10 (nach dem Leerlauf-`review-2026-08-10.md` aus dem Schedule-Lauf 07:34). Fokus laut Auftrag: **Neue Features** (Partei-Seiten, Nachrichten-Feed, Taktik-Simulator Szenarien B/C/D, Teilen-Links) plus Algorithmen (Übereinstimmungsrechnung, Koalitionsberechnung, Themenzuordnung, Sitzverteilung).

Geprüft: `README.md`, `config.json`, `elections.json`, `index.html`, `styles.css`, `script.js`, `einfache-sprache.json`, alle Wahlen unter `elections/<id>/`, vorhandene Reports, `todo.md`/`archived-todo.md`, offene Issues/PRs und Branches.

Bericht wird während des Reviews schrittweise gefüllt.

## Verifiziert (bisher)

- `node --check script.js`: läuft fehlerfrei.
- `node --check tools/tactical-harness.js`, `harness/*`: fehlerfrei.
- Sitzsummen alle 4 Wahlen = `meta.sitze` (per Node).
- Keine ausgeschlossene Koalition in `berechneKoalitionen()` (per Node über alle 4 Wahlen).
- offene Issues/PRs: keine; PR #100 (issue99, Harness in script.js einbetten) ohne Merge geschlossen, Branch `opencode/issue99-20260810091339` verwaist auf origin.

## Befunde

Keine neuen P1-Bugs. 1× P3 (Themen-Fallback), 1× offener GitHub-Cleanup.

### P3 – Verbesserungen

- **`determineTopic()`-Keyword-Fallback klassifiziert anders als das explizite `thema`** – alle Fragen aller 4 Wahlen haben ein valides `thema` (Fallback wird aktuell nie erreicht); der Keyword-Fallback (script.js:2592-2602) würde aber für viele Fragen ein anderes Topic liefern als das redaktionelle `thema` (btw2029 14, LSA 19, Berlin 21, MV 15 Mismatches; z. B. btw2029 #11 Soziales→Wirtschaft, #19 Umwelt→Außenpolitik, #20 Außenpolitik→Wirtschaft, Berlin #21/#28/#46 Umwelt→Digitales). Kein Live-Effekt bei den aktuellen Daten, aber latente Fehlerquelle für künftige Wahlen ohne Pflicht-`thema`. Empfehlung: Fallback entfernen oder an die redaktionellen Topics angleichen.

### GitHub-Cleanup (offen)

- **Verwaister Branch `opencode/issue99-20260810091339`** – PR #100 (issue99, Harness in script.js einbetten) wurde ohne Merge geschlossen, Issue #99 ist geschlossen; der Branch liegt weiterhin verwaist auf origin und sollte gelöscht werden.

## Fazit

Review des Fokusbereichs „Neue Features" abgeschlossen. Keine neuen P1-Bugs; die zentralen Algorithmen (Sitzverteilung, Koalitionen, Übereinstimmungsrechnung, Taktik-Warnungen, Share-Hash-Parsing) sind per Node-Harness gegen die echten Datendateien verifiziert. Offen: P3-Befund zum Themen-Fallback (siehe `todo.md`) und der GitHub-Cleanup des verwaisten issue99-Branches.