# Koalitions-O-Mat – Review vom 2026-08-05

Automatisiertes Code-Review (DeepSeek-V4-Flash) über das gesamte Projekt. Verdachtsfälle wurden per Node gegen die echten Datendateien unter `elections/` verifiziert. Kein App-Code verändert, nur dieser Bericht und `todo.md`.

Hinweis: Der offene PR #32 (`opencode/dispatch-c0b481-20260805222205`) aus einem vorherigen Lauf enthält bereits die hier bestätigte Sitzverteilungs-Analyse; die Befunde sind hier erneut verifiziert und in `todo.md` aufgenommen.

## Verifiziert (per Node gegen die echten Datendateien)

- **Sitzverteilung** (`berechneSitze`, script.js:1824-1846): `meta.verfahren` aus `elections/*/werte.json` wird **nie angewendet**.
  - `berechneSitze()` prüft `verfahren === 'sainteLague'` (script.js:1832/1838), aber die Daten enthalten `'sainte-lague'` (btw2029), `'saintelague'` (berlin-2026) und `'hare-niemeyer'` (ltw-sachsen-anhalt-2026, mv-2026). Kein String matcht → alle 4 Wahlen fallen still auf den `'dhondt'`-Fallback.
  - `'hare-niemeyer'` ist im Code gar nicht implementiert.
  - Sichtbare Auswirkung (Sitzsummen korrekt, Werte falsch):
    - LSA (83): App d'Hondt → `AfD:39 CDU/CSU:22 LINKE:12 SPD:6 GRÜNE:4`; deklariert Hare-Niemeyer → `AfD:38 CDU/CSU:22 LINKE:12 SPD:6 GRÜNE:5` (GRÜNE −1, AfD +1).
    - MV (79): d'Hondt und Hare-Niemeyer fallen hier zufällig identisch aus (`AfD:24 SPD:18 CDU/CSU:17 BSW:11 LINKE:5 GRÜNE:4`).
    - btw2029 (630) / Berlin (130): Sainte-Laguë vs. d'Hondt aktuell zufällig identisch, d. h. der Fehler ist latent und wird sichtbar, sobald sich Umfragewerte ändern.
- **Partei-Abdeckung**: Alle Parteien aus `werte.json` sind in `fragen.json` mit Antworten vertreten (alle 4 Wahlen), keine fehlenden.
- **i18n**: Alle `data-i18n`-Keys aus `index.html` (43) sind in `einfache-sprache.json` → `ui` vorhanden; auch alle statischen `t()`-Aufrufe bis auf `party.newsRetry` (s. u.).
- **Fragen**: `thema`-Feld in allen 4 Wahlen durchgehend gesetzt; Berlin ohne `Außenpolitik`/`Digitales`-Thema (nicht zwingend ein Fehler).

## P1 – Bugs

- [ ] **Sitzverteilung: konfiguriertes Verfahren wird nie angewendet** – `berechneSitze()` (script.js:1824-1846) prüft exakt `'sainteLague'`, die `werte.json`-Werte sind aber `'sainte-lague'`, `'saintelague'` und `'hare-niemeyer'` → alles läuft über `'dhondt'`. Zusätzlich fehlt die Hare-Niemeyer-Implementierung für LSA/MV. Verifiziert: LSA (83) zeigt `GRÜNE:4` statt `5`, `AfD:39` statt `38`. Fix-Vorschlag: Normalisierung der Verfahrensnamen in `berechneSitze()` (z. B. Lowercase + Entfernen von `-`) plus Largest-Remainder-Zweig; oder die Strings in den 4 `werte.json` vereinheitlichen und an den Check anpassen.

## P2 – Fehlende Features

- [ ] **Hare-Niemeyer/Largest-Remainder nicht implementiert**, obwohl `ltw-sachsen-anhalt-2026/werte.json` und `mv-2026/werte.json` `meta.verfahren: "hare-niemeyer"` deklarieren. Ist ein Ergebnis des P1-Stringmismatchs, aber eigenständig: auch bei korrekter Erkennung würde `berechneSitze()` den Zweig fehlen.

## P3 – Verbesserungen

- [ ] **`meta.verfahren`-Werte inkonsistent** – `'sainte-lague'` (btw2029), `'saintelague'` (berlin-2026), `'hare-niemeyer'` (LSA, MV) sind unterschiedlich geschrieben (Trennzeichen, Groß-/Kleinschreibung). Einheitliche Nomenklatur (z. B. `sainte-lague`/`dhondt`/`hare-niemeyer`) dokumentieren oder im Code normalisieren.
- [ ] **Parteifarben: SPD und BSW teilen sich `#E3000F`** – `config.json:4` (SPD) und `config.json:9` (BSW) identisch. In Charts und Partei-Seiten (btw2029: SPD 12 %, BSW 4 %; Berlin: beide vertreten) sind die beiden Parteien farblich nicht unterscheidbar. Eigene Farbe für BSW vergeben (z. B. kräftiges Rot-Orange).
- [ ] **`party.newsRetry` fehlt in `einfache-sprache.json`** – `loadPartyNews()` (script.js:723) nutzt `t('party.newsRetry', …)`; der Key existiert nicht in `ui` (alle übrigen `party.*`-Keys sind vorhanden). Der „Erneut versuchen"-Button der Nachrichten-Sektion bleibt in einfacher Sprache unübersetzt.

## Weiterhin offen / bestätigt (keine neuen Befunde, nur re-verifiziert)

- **„Fortsetzen" zeigt nach Reload Frage 1 statt der gespeicherten Position** – weiter offen: `initializeTest()` (script.js:1091) setzt `active` weiterhin nur bei `i === 0`, nicht bei der wiederhergestellten `currentQuestion`. (Bereits als P1 in `todo.md` erfasst.)
- Alle übrigen offenen Punkte aus `todo.md` (MV-GRÜNE-5-%-Frage, Ranking-Normalisierung, Umfragewert-Gewichtung, harte `aria-label`s, `partyFilter`-Leerlauf, Koalitions-Share-Link, Wahlwechsel-Tab, Ladefehler-Hinweis, „Kultur"-Thema, Datenqualität CDU/CSU vs. CDU, SSW-Berlin, Tap-Ziele) wurden geprüft und bleiben gültig.
