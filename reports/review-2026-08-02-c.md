# Koalitions-O-Mat – Review vom 2026-08-02 (3. Lauf, MD-Abgleich)

Automatisiertes Code-Review (dritter Lauf am 2026-08-02, HEAD `183fea0`, Issue #13 „Md files Outdated"). Fokus: Abgleich von `README.md` und `todo.md` mit dem tatsächlichen Code-Stand sowie Zusammenführung aller Befunde aus `reports/review-2026-08-01.md`, `reports/review-2026-08-02.md` und `reports/review-2026-08-02-b.md` in `todo.md`. Alle Status-Aussagen per Node gegen die echten Datendateien und gegen `script.js` verifiziert.

**Kurzbefund:** Die seit dem 2. Lauf umgesetzten Fixes sind im Code vorhanden (paarweise Übereinstimmung, `cancelPendingAdvance()`, Empty-Guards, `showChartPlaceholder()`, resumeHint, kompakte Teilen-URL). Zwei P1-Einträge in `todo.md` (2. Lauf) waren veraltet und sind jetzt als erledigt markiert. Die Einfachsprache-Abdeckung beträgt inzwischen 170 Fragen (45+40+52+33) – der README-Text „125 Fragen" und der P2-Eintrag „Kein Hinweis auf fehlende einfache Sprache bei btw2029" waren dadurch falsch. README wurde entsprechend aktualisiert. Neu in `todo.md` übernommen wurden drei Befunde aus den Vor-Reviews, die dort bisher fehlten (`noPartyInfo`-Doppel-Fallback, Teilen-Link verliert neutrale Antworten, Parteien mit wenigen beantworteten Fragen verzerren Ergebnisliste).

## P1 – Bugs

- [x] **`pendingAdvanceTimer`-Race beim Wahlwechsel** (todo 2. Lauf, war fälschlich offen) – gefixt: `resetTest()` ruft `cancelPendingAdvance()` (script.js:556), `setActiveElection()` ruft `resetTest()` (script.js:236); zusätzlich clearen `skipQuestion()`/`showNextQuestion()`/`showPreviousQuestion()` den Timer.
- [x] **`createStatsSummary()` TypeError bei leerem `umfragewerte`** (todo 2. Lauf, war fälschlich offen) – gefixt durch Empty-Guard (script.js:1088-1091) + `reduce` nur noch auf nicht-leerer Liste.
- [ ] **Einfache Sprache wird beim Reload nicht angewendet** (2. Lauf, weiterhin offen) – verifiziert: `applySavedSimpleLang()` (script.js:1435) läuft im `DOMContentLoaded`-Handler vor dem asynchronen Fetch von `einfache-sprache.json` (`loadElections()`, script.js:289-291); `applyStaticI18n()` wird nach dem Laden nirgends erneut aufgerufen. Fix: nach `loadElections()` erneut anwenden.
- [ ] **`berechneSitze()`-Befund präzisiert** (2. Lauf) – die NaN-Aussage stimmt nicht mehr: bei keiner Partei über der Sperrklausel liefert `berechneSitze()` (script.js:1300-1318) ein leeres Array (keine Division durch `gueltig=0`, da `above` leer ist). Es fehlt aber weiterhin ein Leer-Guard in `createSeatChart()` (script.js:1128-1146) → leeres Pie-Chart mit „0 Sitze" statt Empty-State.
- [ ] **Rein neutrale Nutzer-Antworten → 0 % für alle Parteien trotz „X/X Fragen beantwortet"** (Nachtrag, weiterhin offen) – `showTestResults()` (script.js:822) überspringt `m` (`if (!ua || ua === 'm') return`), `totalAnswered` (script.js:847) zählt `m` aber mit; verifiziert: 45×`m` → alle Parteien 0,0 % bei Anzeige „45/45 Fragen beantwortet". Betrifft ebenso `berechneUserMatchFuerKoalition()` (script.js:477) und `berechneUserMatchNachThema()` (script.js:731).
- [ ] **`noPartyInfo`-Key mit zwei unterschiedlichen Fallback-Texten** (aus review-2026-08-01, neu in `todo.md` übernommen) – `initializeParteienPage()` (script.js:386 vs. 393): derselbe `t()`-Key, einmal „Keine Parteidaten für diese Wahl verfügbar.", einmal „Weitere Informationen zu dieser Partei folgen." → inkonsistent zwischen Normal- und Einfachsprache-Modus.

## P1 – Algorithmus

- [x] **Paarweise Übereinstimmung verifiziert** – `berechneUebereinstimmung()` (script.js:445-468) liefert per Node gegen die echten Daten: btw2029 max 50,9 % (11/11 ≥ 20), ltw-sachsen-anhalt 52,0 % (6/6), berlin-2026 48,0 % (9/9), mv-2026 58,7 % (11/11). Koalitionen-Tab bei Default-Schwelle 20 in allen 4 Wahlen gefüllt. Der Fix aus dem 2. Lauf hält.
- [ ] **Sitzverteilung nutzt Largest-Remainder statt d'Hondt/Sainte-Laguë** (2. Lauf, weiterhin offen) – Bundestag = Sainte-Laguë, Landtagswahlen meist d'Hondt; verifiziert weicht ltw-sachsen-anhalt um 1 Sitz ab. Verfahren pro Wahl konfigurierbar machen.

## P1 – Einfache Sprache / i18n

- [x] **btw2029 ohne einfache Sprache** – erledigt: `einfache-sprache.json` übersetzt inzwischen alle 45 btw2029-Fragen; Gesamtabdeckung 170 (45+40+52+33). Der P2-Eintrag „Kein Hinweis auf fehlende einfache Sprache bei btw2029" aus dem 2. Lauf ist damit gegenstandslos (markiert).
- [ ] **Hartkodierte deutsche Strings ohne `t()`** (weiterhin offen) – „Quelle: " (script.js:629), Tabellenkopf „Frage/Sie" (script.js:752), Legende „✓ Zustimmung / ✗ Ablehnung / — Nicht vergleichbar" (script.js:767), Toggle-Label „Einfache Sprache" (index.html:22). Der Platzhalter „Test durchführen…" ist dagegen über `topicChartEmpty` gelöst.

## P2 – Fehlende Features

- [ ] **„Fortsetzen" springt nach Reload immer zu Frage 1** (2. Lauf, weiterhin offen) – `saveTestState()` (script.js:55-62) speichert nur `answers` + `important`, nicht `currentQuestion`; `initializeTest()` (script.js:593) startet wieder bei 0. Der resumeHint zeigt zwar „Fortgesetzt: X von Y", die Position wird aber nicht wiederhergestellt.
- [ ] **`saveTestResult()` speichert auch bei 0 verwertbaren Antworten** (Nachtrag, weiterhin offen) – `showTestResults()` (script.js:951) ruft `saveTestResult()` ohne Guard; komplett übersprungener Test landet als „0,0 %"-Eintrag in der Historie und verdrängt via `createTopicChart()` (script.js:1198, `.pop()`) das letzte echte Ergebnis.
- [ ] **Teilen-Link verliert neutrale Antworten** (aus review-2026-08-01, neu in `todo.md` übernommen) – `shareResults()` (script.js:82) filtert `m`-Antworten heraus; nach `applyPendingShare()` fehlen sie in `userAnswers` → „Fragen beantwortet"-Zähler und Ergebnis weichen vom Original ab.
- [ ] **Parteien mit wenigen beantworteten Fragen verzerren Ergebnisliste** (aus review-2026-08-01, neu in `todo.md` übernommen) – Berlin: Volt/Tierschutz antworten nur auf 3/52 Fragen, erscheinen aber mit hohem Match im Ranking (Paar GRÜNE+Volt = 100 %). Mindest-Abdeckung oder Hinweis „nur X von N Fragen beantwortet".
- [ ] **„Ergebnis teilen"-Button im Koalitionen-Tab teilt den Test, nicht die Koalitions-Sicht** (Nachtrag, weiterhin offen) – `shareResults()` (script.js:81) kodiert nur Antworten + wichtige Fragen; Filter (Mindestmatch, Ausschlüsse, Typ) gehen verloren.
- [ ] **Partei-Filter-Dropdown inkonsistent mit dem Ergebnis** (Nachtrag, weiterhin offen) – FDP/BSW (btw2029) antworten auf Fragen und erscheinen im Ergebnis, fehlen aber im Filter (script.js:352, nur ≥ Sperrklausel); die Ausschluss-Checkboxen enthalten sie dagegen.
- [ ] **Cross-Election-Leak bei fehlgeschlagenem `fragen.json`** (Nachtrag, weiterhin offen) – `setActiveElection()` (script.js:211) `window.parteienData = data.fragen || window.parteienData` behält bei Ladefehler die Fragen der vorherigen Wahl; auf null zurücksetzen.

## P3 – Verbesserungen

- [x] **README „125 Fragen in einfacher Sprache" korrigiert** – `README.md` Z. 14 jetzt „alle 170 Fragen" (45+40+52+33); Zusatz-Doku für neue Features (Teilen-Link, Fortsetzen-Hinweis, Antworten zurücksetzen, paarweiser Koalitionsalgorithmus) ergänzt.
- [x] **`reports/*.md` in `todo.md` übernommen** – alle offenen Befunde aus review-2026-08-01/-02/-02-b sind jetzt in `todo.md` vertreten (inkl. der drei zuvor fehlenden); Status verifiziert.
- [ ] **Paar-Durchschnitt verdeckt Fundamentalkonflikte** (Nachtrag, weiterhin offen) – verifiziert: AfD+CDU/CSU+GRÜNE = 33,7 % interne Übereinstimmung bei AfD–GRÜNE = 0 % und CDU/CSU–GRÜNE = 10,3 %; Minimum-Paar-Agreement zusätzlich anzeigen.
- [ ] **`berechneUebereinstimmung()` ohne Umfragewert-Gewichtung** (Nachtrag, weiterhin offen) – kleine Parteien zählen im Paarvergleich wie große; Gewichtung nur in `berechneUserMatchFuerKoalition()` (script.js:482).
- [ ] **10 Fragen in „Sonstiges" (Kultur/Ehrenamt/Kirchen/Rundfunk/Schwimmbäder/Gartenschau/Tanzverbot)** (Nachtrag, weiterhin offen) – verifiziert: ltw 6, Berlin 3, btw2029 1; eigene Kategorie „Kultur" oder Zuordnung „Soziales".
- [ ] **`maxCoalitionSize: 5` in `elections/ltw-sachsen-anhalt-2026/config.json` abweichend** (Nachtrag, weiterhin offen) – alle anderen 3 Wahlen nutzen 4; funktional wirkungslos (nur 4 Parteien ≥ 5 %), aber Dateninkonsistenz.
- [ ] **Datenqualität: „CDU/CSU" statt „CDU" in Berlin/MV/LSA** (2. Lauf, weiterhin offen) – `partyColors` (config.json:3) kennt kein „CDU"; „SSW" (0,5 %) in der Berlin-Umfrage unplausibel (nur Schleswig-Holstein).
- [ ] **Ausschluss-Checkboxen für Parteien < 5 % wirkungslos** (2. Lauf, weiterhin offen) – `populatePartyDropdowns()` (script.js:357-365) listet antwortende Parteien unter der Sperrklausel, `berechneKoalitionen()` (script.js:422-424) ignoriert sie.
- [ ] **Fallback `data.fragen || window.parteienData` mischt Wahlen** (2. Lauf, weiterhin offen) – script.js:211; stattdessen Leer-State + Fehlermeldung.
- [ ] **`welcome-card-type` nicht i18n** (2. Lauf, weiterhin offen) – `renderWelcomeCards()` (script.js:266) gibt `e.type` roh aus.
- [ ] **`minMatch`-Slider-Label initial „0 %"** (Nachtrag, weiterhin offen) – index.html:102 bleibt bis zum ersten `updateKoalitionen()` sichtbar.
- [ ] **`deleteTestHistoryEntry()`/`clearTestHistory()` zeichnen `createTopicChart()` bei verstecktem Daten-Tab** (Nachtrag, weiterhin offen) – echarts.init auf `display:none`-Container (script.js:1031, 1037).
- [ ] **ECharts-CDN ohne Fallback** (Nachtrag, weiterhin offen) – `echarts.init` (script.js:779, 1052) wirft bei CDN-Ausfall; Fehlermeldung/Offline-Hinweis ergänzen.
- [ ] **„Beste Koalition für Sie" ignoriert Ausschluss-Filter des Koalitionen-Tabs** (Nachtrag, weiterhin offen) – `showTestResults()` (script.js:881) ruft `berechneKoalitionen('beide')` ohne `excludeParties`.
- [ ] **`createCoalitionPotentialChart()` sortiert den `koalitionenCache` in-place** (Nachtrag, weiterhin offen) – script.js:1149-1151.

---

## Prüfprotokoll (Verifikation per Node, HEAD `183fea0`)

- `node --check script.js` OK; alle JSON-Dateien valide.
- Einfachsprache-Abdeckung: 170 Fragen (btw2029 45, ltw 40, Berlin 52, MV 33) – README-Anspruch jetzt korrekt.
- Mehrheitskoalitionen mit paarweiser Übereinstimmung (maxSize aus Config): btw2029 11 (max 50,9 %), ltw 6 (max 52,0 %), Berlin 9 (max 48,0 %), MV 11 (max 58,7 %); alle ≥ 20 %.
- Paar-Agreements btw2029: AfD–GRÜNE 0,0 %, CDU/CSU–GRÜNE 10,3 %, AfD+CDU/CSU+GRÜNE 33,7 % (Fundamentalkonflikt im Paar-Durchschnitt verdeckt).
- `berechneSitze()` mit leerer/unter-Schwelle-Liste: leeres Array, kein NaN.
- `shareResults()` filtert `m`-Antworten; `parseShareHash()` akzeptiert Alt-/Neuformat, kann fehlende `m` aber nicht rekonstruieren.
- Datenkonsistenz: keine antwortende Partei ohne `werte`-Eintrag; `Sonstiges`-Zählung 1/6/3/0 wie im Nachtrag.
- i18n: alle `data-i18n`-Keys und `t()`-Keys in `einfache-sprache.json` vorhanden; keine ungenutzten Keys.
