# Koalitions-O-Mat – Review vom 2026-08-01

Automatisiertes Code-Review des gesamten Projekts. Alle Befunde gegen echte Datendateien verifiziert (Node/Python). Frühere Befunde aus `todo.md` werden nicht wiederholt, sondern fortgeschrieben.

**Status:** Die meisten Befunde wurden in Commit `03c6ad8` behoben (Stand 2026-08-01). Noch offen: btw2029 ohne einfache Sprache und „Parteien & Kandidaten" ohne Daten.

## P1 – Bugs

- [x] **Sitzverteilung zeigt für alle Wahlen 736 Sitze statt 630/87/130/71** – `setActiveElection` (script.js:208-210) übernimmt `meta.sitze` aus `werte.json` in `config.meta.gesamtSitze`; `berechneSitze()` (script.js:1211-1212) fällt auf `meta.sitze` zurück. Behoben in `03c6ad8`.
- [x] **Absturz bei fehlerhaftem Teilen-Link** – `parseShareHash()` (script.js:100-105) umschließt `decodeURIComponent` mit try/catch und gibt bei Fehler `null` zurück. Behoben in `03c6ad8`.
- [x] **Einfache-Sprache-Umschalten im Ergebnis erzeugt doppelte Verlaufseinträge** – `toggleSimpleLanguage()` (script.js:1304-1306) setzt `suppressHistorySave` beim erneuten `showTestResults()`. Behoben in `03c6ad8`.
- [x] **Antwortänderung überspringt Frage** – `selectAnswer()` (script.js:614) löscht `pendingAdvanceTimer` vor dem Neu-Planen; Doppel-Timer unmöglich. Behoben in `03c6ad8`.

## P1 – Algorithmus

- [x] **Standard-Schwelle `minMatchForCoalition: 40` unkalibriert → Kernfunktion standardmäßig leer** – Default auf 20 gesenkt in `config.json` und allen `elections/*/config.json`. Behoben in `03c6ad8`.

## P1 – Einfache Sprache

- [ ] **btw2029 hat keine einfache Sprache** – `einfache-sprache.json` deckt nur ltw-sachsen-anhalt-2026 (40), berlin-2026 (52), mv-2026 (33) ab; die Standard-Wahl btw2029 (45 Fragen) fällt beim Umschalten komplett auf Normaltext zurück. `simpleQuestionText()` (script.js:19) fällt still auf `f[field]` zurück → inkonsistentes Verhalten zwischen Wahlen, README-Zusage „125 Fragen" deckt die Bundestagswahl nicht ab.

## P2 – Fehlende Features / Usability

- [x] **Partei-Filter listet Parteien, die nie in Koalitionen erscheinen** – `populatePartyDropdowns()` (script.js:332-340) nimmt nun auch antwortende Parteien unter der Sperrklausel auf („answeredParties"). Behoben in `03c6ad8`.
- [ ] **„Parteien & Kandidaten" ohne Daten** – in allen 4 `werte.json` fehlen `beschreibung`, `kandidaten`, `website`; `initializeParteienPage()` (script.js:373) zeigt nur Umfragewerte + Platzhalter „Mehr Infos folgen" → README-Feature (Programmbeschreibungen, Kandidaten, Websites) faktisch nicht vorhanden.

## P3 – Verbesserungen

- [x] **`berechneUserMatch()` ist toter Code** – entfernt. Behoben in `03c6ad8`.
- [x] **Einfache-Sprache-Toggle verliert Testposition** – `toggleSimpleLanguage()` (script.js:1308-1314) merkt sich `currentQuestion` und stellt sie nach dem Neu-Rendern wieder her. Behoben in `03c6ad8`.
- [x] **Doppelte Sitz-Namenskonvention** – eine Quelle: `meta.sitze` wird in `setActiveElection` übernommen. Behoben in `03c6ad8`.
- [x] **`createPartyOverviewChart()` ohne Leer-Guard** – Leer-Guard mit `noData`-Text (script.js:1018-1023). Behoben in `03c6ad8`.
- [x] **Themenverteilung-Chart missverständlich** – `createTopicChart()` (script.js:1103) nutzt jetzt Balken mit 0-100-Achse statt `roseType:'radius'`. Behoben in `03c6ad8`.

---

### Prüfprotokoll (Verifikation)

- `berechneSitze` mit `gesamtSitze=736` vs. `meta.sitze`: alle 4 Wahlen zeigen 736 statt Soll.
- Interne Übereinstimmung aller Mehrheitskoalitionen (2–4 Parteien) per Node: btw2029 max 28,1 %, Berlin max 9,1 %, MV max 40,9 %, Sachsen-Anhalt max 51,2 %.
- i18n: alle in `index.html`/`script.js` genutzten `t()`-Keys existieren in `einfache-sprache.json` (keine fehlenden/ungenutzten Keys).
- Parteien-Konsistenz: keine Partei in `fragen.json` fehlt in `werte.json`; alle Parteienfarben in `config.json` vorhanden.
- `thema`-Felder: alle gültig, keine ungültigen Topics.
