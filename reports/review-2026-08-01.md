# Koalitions-O-Mat – Review vom 2026-08-01

Automatisiertes Code-Review des gesamten Projekts. Alle Befunde gegen echte Datendateien verifiziert (Node/Python). Frühere Befunde aus `todo.md` werden nicht wiederholt, sondern fortgeschrieben.

## P1 – Bugs

- [ ] **Sitzverteilung zeigt für alle Wahlen 736 Sitze statt 630/87/130/71** – `berechneSitze()` (script.js:1209) liest `config.meta.gesamtSitze`, das nie befüllt wird: `setActiveElection` (script.js:198-201) übernimmt nur `sperrklausel` aus `werte.json`-`meta.sitze`, `elections/<id>/config.json` enthält kein `meta`. Verifiziert per Node: Soll 630/87/130/71, ausgegeben wird immer 736. (Behauptung in todo.md „gefixt, Test 630/630" trifft auf den aktuellen Code nicht zu.)
- [ ] **Absturz bei fehlerhaftem Teilen-Link** – `parseShareHash()` (script.js:99) ruft `decodeURIComponent(location.hash)` ohne try/catch; Aufruf mit `#w=…%zz` wirft URIError im `DOMContentLoaded`-Handler (script.js:1326) → App bootet nicht, Loading-Overlay bleibt hängen. Verifiziert per Node.
- [ ] **Einfache-Sprache-Umschalten im Ergebnis erzeugt doppelte Verlaufseinträge** – `toggleSimpleLanguage()` (script.js:1300) ruft bei sichtbarem Ergebnis `showTestResults()` erneut auf → `saveTestResult()` (script.js:912) pusht ohne `suppressHistorySave` einen neuen Eintrag und zeigt erneut „Ergebnis gespeichert!". Jedes Umschalten dupliziert die Historie.
- [ ] **Antwortänderung überspringt Frage** – `selectAnswer()` (script.js:596) plant `showNextQuestion()` via `setTimeout(300)`; wer innerhalb von 300 ms dieselbe Frage anders beantwortet (Tastatur 1→3, Doppelklick), feuert zwei Timer → aktuelle Frage wird übersprungen. Bei der letzten Frage feuert `showTestResults()` doppelt (auch doppelte Historie).

## P1 – Algorithmus

- [ ] **Standard-Schwelle `minMatchForCoalition: 40` unkalibriert → Kernfunktion standardmäßig leer** – die interne Übereinstimmung erreicht real max. 28,1 % (btw2029) bzw. 9,1 % (Berlin); verifiziert per Node: 0 von 11 (btw2029) bzw. 0 von 9 (Berlin) Mehrheitskoalitionen ≥ 40 %. Folgen: Koalitionen-Tab zeigt bei Default-Einstellung „Keine passenden Koalitionen" und die Sektion „Beste Koalition für Sie" (script.js:803-805) erscheint bei der Standard-Wahl btw2029 nie. Entweder Metrik (z. B. auch „m"-Antworten als Zustimmung/relative Nähe) oder Default-Schwelle (z. B. 20) anpassen.

## P1 – Einfache Sprache

- [ ] **btw2029 hat keine einfache Sprache** – `einfache-sprache.json` deckt nur ltw-sachsen-anhalt-2026 (40), berlin-2026 (52), mv-2026 (33) ab; die Standard-Wahl btw2029 (45 Fragen) fällt beim Umschalten komplett auf Normaltext zurück. `simpleQuestionText()` (script.js:19) fällt still auf `f[field]` zurück → inkonsistentes Verhalten zwischen Wahlen, README-Zusage „125 Fragen" deckt die Bundestagswahl nicht ab.

## P2 – Fehlende Features / Usability

- [ ] **Partei-Filter listet Parteien, die nie in Koalitionen erscheinen** – `populatePartyDropdowns()` (script.js:324-330) nimmt alle antwortenden Parteien (inkl. < Sperrklausel), `berechneKoalitionen()` (script.js:403) filtert sie heraus → Auswahl „FDP"/„BSW" (btw2029) ergibt immer „Keine passenden Koalitionen gefunden." (gleiches für GRÜNE/FDP in Sachsen-Anhalt etc.).
- [ ] **„Parteien & Kandidaten" ohne Daten** – in allen 4 `werte.json` fehlen `beschreibung`, `kandidaten`, `website`; `initializeParteienPage()` (script.js:372-380) zeigt nur Umfragewerte + Platzhalter „Mehr Infos folgen" → README-Feature (Programmbeschreibungen, Kandidaten, Websites) faktisch nicht vorhanden.

## P3 – Verbesserungen

- [ ] **`berechneUserMatch()` ist toter Code** – script.js:898 wird nirgends aufgerufen; entweder in `showTestResults` nutzen oder löschen.
- [ ] **Einfache-Sprache-Toggle verliert Testposition** – `toggleSimpleLanguage()` (script.js:1302) ruft `initializeTest()` (setzt `currentQuestion = 0`, script.js:541) → Sprung zurück zu Frage 1; Position vor dem Neu-Rendern merken.
- [ ] **Doppelte Sitz-Namenskonvention** – `werte.json` nutzt `meta.sitze`, Script erwartet `config.meta.gesamtSitze` (script.js:1209); eine Quelle festlegen und in `setActiveElection` übernehmen.
- [ ] **`createPartyOverviewChart()` ohne Leer-Guard** – bei leerem/unter-1 %-Datensatz liefert `Math.max(...parties.map(…))` (script.js:1015) -Infinity → fehlerhafte Achse.
- [ ] **Themenverteilung-Chart missverständlich** – `createTopicChart()` (script.js:1113) mappt j/n/m auf 100/0/50 und nutzt `roseType: 'radius'`: „Neutral" erscheint als Kreisfläche, die Werte sind Positionswerte, keine Häufigkeiten; Beschriftung/Achse klären oder Balken-Darstellung.

---

### Prüfprotokoll (Verifikation)

- `berechneSitze` mit `gesamtSitze=736` vs. `meta.sitze`: alle 4 Wahlen zeigen 736 statt Soll.
- Interne Übereinstimmung aller Mehrheitskoalitionen (2–4 Parteien) per Node: btw2029 max 28,1 %, Berlin max 9,1 %, MV max 40,9 %, Sachsen-Anhalt max 51,2 %.
- i18n: alle in `index.html`/`script.js` genutzten `t()`-Keys existieren in `einfache-sprache.json` (keine fehlenden/ungenutzten Keys).
- Parteien-Konsistenz: keine Partei in `fragen.json` fehlt in `werte.json`; alle Parteienfarben in `config.json` vorhanden.
- `thema`-Felder: alle gültig, keine ungültigen Topics.
