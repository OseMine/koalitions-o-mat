# Review vom 2026-08-08 (gesamtes Projekt, Node-Verifikation + GitHub-Cleanup)

Alle Befunde per Node-Harness gegen die echten Datendateien verifiziert (`node --check script.js` OK, alle 4 Wahlen). **Keine neuen P1-Bugs.**

## P1 – Bugs

Keine neuen P1-Befunde. `node --check script.js` OK; Sitzsummen, Koalitionswerte und Ranking-Verhalten re-verifiziert.

## P2 – Fehlende Features

Keine neuen P2-Befunde.

## P3 – Verbesserungen

- [ ] **Berlin-2026: BSW hat bei 6 Fragen keine Position** (nr 9, 20, 32, 36, 37, 45) – einzige Partei über alle 4 Wahlen mit Lücken: `getAnswerValue()` liefert `'m'` (neutral) → BSW erscheint dort als „Neutral" und mit „Nur 46 von 52 Fragen beantwortet". Entweder Positionen ergänzen (Datenqualität) oder als bewusste Auslassung dokumentieren (Design-Entscheidung).

## Verifiziert

- **Sitzverteilung** (alle 4 Wahlen): btw2029 630 (sainte-lague), Berlin 130 (saintelague), LSA 83 (hare-niemeyer), MV 79 (hare-niemeyer) – alle Summen = `meta.sitze`.
- **Ranking-Normalisierung**: `normalisiereUebereinstimmung()` dämpft Werte unter `minAnswersForRanking` (5) zur 50-%-Baseline; keine Partei erreicht mehr 100 % mit wenigen Antworten. btw2029 (1×„j"): SPD/GRÜNE/LINKE/BSW 60 %, CDU/FDP/AfD 40 %.
- **Koalitionsberechnung** unverändert plausibel (alle-j-Szenario: Werte 41–90 %, sortiert nach Übereinstimmung).
- **i18n**: alle `t()`- und `data-i18n`/`data-i18n-aria`-Keys vorhanden; `aria.simpleLangToggle`/`aria.themeToggle`/`aria.githubLink` zentralisiert (index.html:24-26).
- **MV-Einfach-Sprache**: `einfache-sprache.json.parteien.mv-2026` deckt alle 7 MV-Parteien ab.
- **Daten-Integrität**: Parteimengen `fragen.json` ↔ `werte.json` je Wahl identisch; alle `thema` gültig; `config.topics` enthält „Kultur"; `}function`-Kosmetik (0 Treffer) bereits behoben (PR #56).
- **Schwellen-Design** (Issue #52): `aria-label`s via `data-i18n-aria` + `t()` (script.js:2107-2113); leere `partyFilter`-Ergebnisse erklärt via `noCoalitionsBelow`/`noCoalitionsFilter` (script.js:1105-1116); MV-GRÜNE-5-%-Entscheidung dokumentiert (script.js:974-975, `>=`-Check).

## Tracking offene GitHub-Issues

- #51 `newsItemMatchesParty()`-False-Positives – weiter offen (P3-Codeverbesserung, keine Regression).
- #52 Daten-/Design-Punkte (MV GRÜNE 5 %, aria-labels, partyFilter) – **erledigt, geschlossen** (alle Akzeptanzkriterien erfüllt, siehe oben).
- #53 Partei-Seite Teilen-Link – **nicht reproduzierbar; geschlossen** (Ursache: behobene Daten-/Tap-Lücken, kein Code-Unterschied).
- #55 Zeilenreferenzen kosmetisch – **geschlossen** (aktuelle Referenzen aktualisiert; historische Reports sind Momentaufnahmen).
- #62 OpenCode-Prompts in gh Actions – neu offen (Meta, kein App-Code).

## GitHub-Cleanup

- 8 verwaiste Merged-Branches gelöscht: `opencode/dispatch-02dc33-20260806091510`, `opencode/issue27-20260808082857`, `opencode/issue43-20260806100733`, `opencode/issue47-20260808083000`, `opencode/issue48-20260808082920`, `opencode/issue49-20260808082935`, `opencode/issue50-20260808082905`, `opencode/issue54-20260808083048`.
- Issues #52, #53, #55 geschlossen.
